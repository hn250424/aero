import { colors } from "./colors";

const html = `<aero-progress-indeterminate-spinner>
</aero-progress-indeterminate-spinner>
`;

const css = ``;

const js = ``;

export const aeroProgressIndeterminateSpinner = {
	html: html,
	css: css,
	js: js,
};
